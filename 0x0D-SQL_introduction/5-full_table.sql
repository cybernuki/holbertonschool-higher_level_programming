-- This scripts prints the full descipion of the table first table from the database hbtn_0c_0
-- in the MySQL server.
-- It uses SHOW COLUMNS FROM first_table; that is equivalent to DESCIBE first_table;
SHOW COLUMNS FROM first_table;
